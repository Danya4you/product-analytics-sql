[Русский](README.md) · **English**

# SaaS Product Analytics in Pure SQL

An end-to-end product analyst case study on PostgreSQL: from a raw event log to
decisions you could take to a product manager and a CFO.

The data is synthetic and generated by a script in this repository. That is a
deliberate choice: in generated data the true effects are known in advance, so
the SQL can be held to account — it must find what is there and must not find
what isn't. Where the conclusions are limited by the data being artificial, the
write-up says so plainly (see *Limits of this case study* below).

```
signups → trial → payment → subscription life → churn
   ↓        ↓        ↓             ↓               ↓
channels activation plans         MRR         prediction
```

**11,999** customer accounts · **472,000** rows of raw event log · **20 months** ·
**10** analysis blocks · **23** automated data quality checks

---

## What is analysed

| | Question | Query |
|---|---|---|
| 0 | What is wrong with the dump: duplicates, staff accounts, late events | [00_data_hygiene.sql](sql/analysis/00_data_hygiene.sql) |
| 1 | Where users are lost and what activation is worth | [01_funnel_activation.sql](sql/analysis/01_funnel_activation.sql) |
| 2 | User retention and net revenue retention | [02_cohort_retention.sql](sql/analysis/02_cohort_retention.sql) |
| 3 | MRR waterfall: new / expansion / contraction / churn | [03_mrr_movements.sql](sql/analysis/03_mrr_movements.sql) |
| 4 | Channel unit economics: CAC, LTV, payback | [04_unit_economics.sql](sql/analysis/04_unit_economics.sql) |
| 5 | Churn drivers and predicting churn a month ahead | [05_churn_drivers.sql](sql/analysis/05_churn_drivers.sql) |
| 6 | RFM segmentation of paying accounts | [06_rfm_segments.sql](sql/analysis/06_rfm_segments.sql) |
| 7 | Onboarding A/B test: SRM, z-test, MDE | [07_ab_onboarding.sql](sql/analysis/07_ab_onboarding.sql) |
| 8 | Pricing A/B test with a metric trade-off | [08_ab_annual_pricing.sql](sql/analysis/08_ab_annual_pricing.sql) |
| 9 | Feature adoption and the correlation trap | [09_feature_adoption.sql](sql/analysis/09_feature_adoption.sql) |

> **A note on language.** SQL comments, result column headings and the three
> documents in `docs/` are written in Russian. The reasoning behind every
> decision lives in those comments, so this README summarises it rather than
> pointing at it. The schema itself — table, column and function names — is in
> English throughout, so the SQL reads normally without translation.

---

## Five results worth the effort

**A quarter of signups cannot be assigned to any channel.** The channel is not
stored anywhere — it is reconstructed from marketing touches on the device, and
for 23.4% of users no touches survived. They can neither be dropped nor folded
into "direct", so CAC is reported as a range: 4,813–6,290 ₽ for paid search.

**Paid acquisition does not pay back.** 86.8% of the ad budget goes to users who
never pay. Gross profit from paid channels is **−19.3M ₽**; paid social runs at
LTV/CAC 0.35 and pays back in 56 months against an average subscription life of 7.

**The first seven days decide everything.** Activated users — one project plus
three tasks in week one — pay 5.9× more often: **38.9% vs 6.6%**. The single
largest hole in the funnel: 3,188 people confirmed their email and never created
a project.

**A quarter of churn is not a customer decision.** Failed payments account for
25.2% of churned subscriptions and 279k ₽ of lost MRR — and those customers
lived *longer* than the rest. Fixed by a dunning schedule, not by the product.

**Inviting colleagues does almost nothing.** Naively it looks like +15.1pp on
conversion. Within comparable segments, 0.5 to 6.0pp remain and no cell clears
the significance threshold.

---

## Running it

Requires PostgreSQL 14+ (tested on 17.11) and Python 3.8+ with **no third-party
packages at all** — the generator uses only the standard library.

```bash
git clone https://github.com/Danya4you/product-analytics-sql.git
cd product-analytics-sql

export PGUSER=postgres PGPASSWORD=postgres
./scripts/build.sh          # generate, load, build marts, run quality checks
./scripts/run_analysis.sh   # all ten analysis blocks
```

Windows, PowerShell:

```powershell
$env:PGUSER='postgres'; $env:PGPASSWORD='postgres'
.\scripts\build.ps1
.\scripts\run_analysis.ps1
```

No local PostgreSQL? Use Docker:

```bash
docker compose up -d
PGHOST=localhost PGPORT=5433 PGUSER=postgres PGPASSWORD=postgres ./scripts/build.sh
```

A full build takes about four minutes: half a minute to generate, a minute and a
half to load 472,000 rows of raw event log, the rest to build marts. For a quick pass:
`USERS=4000 ./scripts/build.sh`.

The dashboard is rebuilt from the database by a script — its numbers are never
typed in by hand:

```bash
python scripts/build_dashboard.py    # → report/dashboard.html
```

Nothing needs to be run to read the results: the complete query output is in
[report/analysis_output.txt](report/analysis_output.txt) and the dashboard in
[report/dashboard.html](report/dashboard.html).

---

## How it is put together

```
sql/
  00_schema.sql          raw layer `app`: 9 tables, keys, CHECK constraints
  01_load.sql            CSV load via \copy
  marts/                 stg_identity, stg_events, stg_attribution, dim_user, fct_*
  analysis/              the ten analysis blocks
tests/
  data_quality.sql       23 checks; exits non-zero on failure
etl/
  generate_data.py       data generator, zero dependencies
docs/
  data-model.md          data model and three places that are easy to get wrong
  metrics.md             metric dictionary: formula, location, caveat
  findings.md            findings with numbers and recommendations
  performance.md         query plan measurements: what was slow and why
report/
  dashboard.html         dashboard in plain HTML and SVG, no libraries
  analysis_output.txt    complete query output
  explain.txt            query plans produced by scripts/explain.sql
```

Two layers. **`app`** holds the data as a production database and an event
tracker would hand it over. **`marts`** holds what the analyst actually looks at.
Analysis queries read only from `marts`; any query reaching into `app` is a
prompt to ask why the field isn't in a mart yet.

---

## The parts that are not obvious

Working through these is half the value of the repository. Each one is
documented at length in the code itself.

**The acquisition channel is not in the database — it has to be reconstructed.**
The product only knows the device and the marketing touches recorded on it before
signup. The channel is derived by a model (last non-direct click, 30-day window),
for a quarter of users it cannot be derived at all, and a different model — first
touch — gives a different answer. Both are computed side by side so it is visible
how much the conclusion rests on the choice of model.

**Cost of acquisition is assembled from two sources at different grains.** Ad
spend arrives as daily rows per campaign, signups arrive one by one; there is no
shared key. CAC is therefore reported twice — over attributed signups only (an
upper bound) and with unattributed signups distributed across channels (a lower
bound).

**Churn reasons are known for a minority.** Passive churn has no reason at all:
the payment failed, the customer cancelled nothing. Of the rest, fewer than half
fill in the survey, and they fill it in messily — casing, spacing, free text.
Normalisation lives in the mart, and shares are computed against an honest
denominator.

**The raw log is dirty, and the cleaning lives in exactly one place.** Tracker
retries duplicate 1.8% of rows, the dump contains staff accounts, and some events
arrive a day or more late. All of it is handled in `marts.stg_events` rather than
in each report — and quantified in block 00, so the cost of skipping the cleanup
is measured rather than assumed.

**A number carries not just a period but a computation date.** Because of
ingestion lag, a cohort's activation rate computed the moment its window closes
is lower than the final figure. That is why yesterday's report for last month and
today's report for the same month disagree — and it is not a bug.

**MRR movements are derived from the event log, not from differencing monthly
snapshots.** A subscription that arrives and leaves inside one calendar month
vanishes without trace when you difference snapshots — it is zero in both. In
the log it appears as a `convert` + `cancel` pair and lands in both new MRR and
churned MRR. The two methods are reconciled by a dedicated test.

**Retention weeks are counted from each user's signup day, not from the
calendar.** Sliced by ISO weeks, week zero lasts four days for someone who signed
up on Thursday and seven for someone who signed up on Monday. In a cohort table
that reads as a dip that does not exist — and people then explain it with product
reasons.

**Immature cohort cells are excluded explicitly.** A cohort that signed up three
weeks ago cannot show week-8 retention. Without the filter, the column average
drifts down and gets read as the product getting worse.

**Churn is measured per month at risk, not per subscription.** Otherwise a
segment made of recent subscriptions always looks more stable — simply because it
hasn't had time to leave.

**Significance and statistical power are kept apart.** The A/B analysis computes
the minimum detectable effect, and one result turns out to be significant while
the observed effect sits *below* the detection threshold — the case usually
mistaken for a win.

**Correlation is tested against selection.** Section 9 shows an 11.3pp effect
that disappears entirely once comparable segments are compared (p ≈ 0.9).

**The statistics run inside the database.** PostgreSQL 17 has no `erf()` — it
arrived in 18 — so the normal distribution is computed with the Zelen & Severo
approximation: `marts.norm_cdf()`, `marts.z_two_proportions()`,
`marts.ci95_diff_proportions()`.

---

## Data quality checks

`tests/data_quality.sql` holds 23 checks for the things the database cannot
enforce on its own: consistency across tables, agreement with the snapshot date,
and reconciliation between marts and the raw layer. The last one matters most —
a mart that has silently drifted from its source is the worst kind of bug,
because the report built on it still looks plausible.

The key check: the cumulative MRR waterfall must equal an independently computed
monthly snapshot, to the kopek.

One check watches the checks themselves: №15 requires duplicates to still be
present in the raw log. Without them the deduplication would go untested and
would break silently once duplicates returned.

Checks run in CI on every push. GitHub Actions brings up PostgreSQL, builds the
marts and executes all ten analysis blocks; a broken query fails the build.

---

## Limits of this case study

The data is **synthetic**, generated by `etl/generate_data.py`. Which means:

- The effects built into it are real exactly as programmed. The queries finding
  them validates the queries — it is not a discovery about a product.
- The three-task activation threshold is set by the generator, so the
  "tasks → conversion" curve breaks as a step. On real data it would be smooth.
- The data carries only the defects that were deliberately built in: duplicate
  deliveries, staff accounts and ingestion lag. Real dumps also bring typos in
  reference tables, tracking logic changed halfway through the period and events
  backfilled months later. Cleaning is represented here, not exhausted.
- Channel attribution is unambiguous — each user comes from exactly one source.
  Real life is never like that.

What this case study does show honestly is a way of reasoning: how the marts are
built, how the metrics are defended against the usual mistakes, and where the
line runs between "the data shows" and "the data permits".

---

## Tech

PostgreSQL 17 · window functions · `GROUPING SETS` · aggregate `FILTER` ·
`LATERAL` · `DISTINCT ON` · `NTILE` and percentiles · materialised views ·
`generate_series` for calendar grids · SQL functions · Python 3 with no
dependencies · GitHub Actions

Licence: [MIT](LICENSE).
